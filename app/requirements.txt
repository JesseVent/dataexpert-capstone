# Pinned dependencies for the Streamlit Databricks App.
# Deploy with: databricks apps deploy discord-capstone-app --source .
#
# Data + agent both run in-process against Lakebase (psycopg) and the workspace
# LLM gateway (databricks-langchain). No SQL-warehouse connector or HTTP agent
# endpoint is required. Versions resolved together against Python 3.12.

streamlit==1.54.0
pandas==2.3.3
plotly==6.2.0
psycopg[binary]==3.2.4
databricks-langchain==0.8.2
langgraph==1.0.10rc1
langchain-core==1.3.3
sentence-transformers==3.0.1

# MCP server (mcp_server.py) — publishes the same six tools over streamable HTTP.
# The Streamlit app does not import it; it is here so one deployed bundle can
# serve either entrypoint by changing app.yaml's `command`.
mcp==2.0.0
